# OO 3.2 via Apache
sudo su www-data -c 'soffice -headless -accept="socket,host=localhost,port=2002;urp;" &'

# LO 3.5 via Apache
#sudo su www-data -c 'soffice --headless --accept="socket,host=localhost,port=2002;urp;" &'

# OO 3.2 via Flask server 
#soffice -headless -accept="socket,host=localhost,port=2002;urp;" &

